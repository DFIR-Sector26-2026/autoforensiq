# AutoForensiq — Python dependencies
# Install with: pip install -r requirements.txt

# ── LLM APIs ──────────────────────────────────────────────────────────────────
anthropic>=0.25.0          # Anthropic Claude SDK (P1 — classifier + report gen)
openai>=1.30.0             # OpenAI SDK (P1 — alternate LLM provider)

# ── Config & CLI ──────────────────────────────────────────────────────────────
pyyaml>=6.0                # config.yaml parsing

# ── JSON Schema validation ────────────────────────────────────────────────────
jsonschema>=4.21.0         # validate case_context.json against schema

# ── ML / XAI (P5) ─────────────────────────────────────────────────────────────
scikit-learn>=1.4.0        # IsolationForest anomaly detector
shap>=0.45.0               # SHAP feature attribution
numpy>=1.26.0,<2.0.0        # pin below 2.0 — scipy/sklearn compiled against NumPy 1.x

# ── Forensic tool support ─────────────────────────────────────────────────────
volatility3>=2.5.0         # Volatility 3 memory analysis (P3)
memprocfs>=5.17.5          # MemProcFS Python API — memory analysis (P3); Linux/Windows x64 only
plaso>=20231224            # log2timeline/psort — timeline analysis (P3); heavy install (dfvfs, dfwinreg, …)

# ── Report output ─────────────────────────────────────────────────────────────
# reportlab>=4.1.0         # (optional) PDF generation — uncomment for PDF output

# ── GUI ───────────────────────────────────────────────────────────────────────
customtkinter>=5.2.0       # modern dark-themed Tkinter widgets (launcher GUI)

# ── Test / dev ───────────────────────────────────────────────────────────────
pytest>=8.0.0              # local test suite
